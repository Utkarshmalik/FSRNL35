

const userTypes={
    customer:"CUSTOMER",
    admin:"ADMIN"
}

const userStatus={
    pending:"PENDING",
    approved:"APPROVED",
    rejected:"REJECTED"
}

module.exports={
    userTypes,
    userStatus
}